To Start Server:
  make server

Practices:
  lets commit to two space tab for indentation

Installing Modules:
  To install modules use: 
    npm install <module name> --save
  REMEMBER ALWAYS USE --save
  The modules will be installed to ./node_modlues which will be not included in the repository

  To install all project modules use:
    make setup
  
  Code in lib are unmanaged dependencies.  They need to be manually updated.

Testing:
  To run unit tests:
    make unit OR
    make test
  to run acceptance tests:
    make features
  to run tests in a specific folder:
    make test FOLDER=test/folder
    
    
